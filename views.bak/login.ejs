<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login - Attendance Mailer</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-100 min-h-screen grid place-items-center">
  <form method="POST" action="/login" class="bg-white p-6 rounded shadow w-96 space-y-3">
    <h1 class="text-xl font-semibold">Admin Login</h1>
    <% if (error) { %>
      <div class="bg-red-100 border border-red-200 text-red-700 px-3 py-2 rounded"><%= error %></div>
    <% } %>
    <div><label class="block text-sm">Username</label><input name="username" class="border rounded w-full px-2 py-1" required value="admin" /></div>
    <div><label class="block text-sm">Password</label><input name="password" type="password" class="border rounded w-full px-2 py-1" required value="admin" /></div>
    <button class="bg-blue-600 hover:bg-blue-700 text-white rounded px-4 py-2 w-full">Login</button>
  </form>
</body>
</html>
